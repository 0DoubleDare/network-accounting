# network-accounting

веб-приложение для учёта сетевой инфраструктуры

## Установка

Если у вас по какой-либо причине сломан проект, запустите его через docker .

1. Установите сам Docker с оф. сайта
2. В терминале (проверьте находитесь ли вы в папке проекта)
3. Введите команды
    1. ```docker compose up -d --build```
    2. Чтобы отключить, введите ```docker compose dowm```

## Важное

### Правила работы с ветками

НЕ пушьте свои изменения в главную ветку main! Для этого сущетсвует ветка develop

> [!TIP]
>
> **Помечайте свои ветки.**
> Если исправляйте баг, пишите название ветки **bugfix/example**
>
> Для документации - **docs/example**
>
> Для новых функций **features/example**
>
> Если улучшайте код, то есть производите рефакторинг - **refactor/example**
>
> Вместо example пишите конкретное назначение вашей ветки, к примеру если обновляйте документацию README.md то ветка
> будет называться
> **docs/update_readme**. Если исправляйте баг с подключением к
> базе данных то ветка будет называться **bugfix/fix_db_connection**
>
> Это нужно для удобства пользования репозиторием в команде

## Правила верстки и стилизации

В каждом файле view`шки должен быть подключен header.php и footer.php в начале и в конце страницы соотвественно.
Стилизуем весь сайт с помощью Bootstrap 5. Акцентный свет - синий

## Переменные

### Работа с сессиями

id пользователя должен храниться в $_SESSION['user_info']['id']

роль пользователя в $_SESSION['user_info']['role']

логин пользователя в $_SESSION['user_info']['login']
